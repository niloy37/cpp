#include "Sorted.h"
#include "ItemType.h"
using namespace std;
int main () {
ItemType i1,i21,i3,i4;
  i1.initialize(1);
  i2.initialize(2);
  i3.initialize(3);
  i4.initialize(4);
  
  SortedType list1;
  list1.putItem(i1);
  list1.putItem(i4);
  list1.putItem(i2);

}
